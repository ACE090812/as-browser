-- Step 2 of adding a site. Copy this to sites/<yourname>/config.lua if your site has settings of its
-- own (prices, text, webhooks). Anything in this file is SERVER ONLY. Remove this file if not needed.
Config.example = {
    greeting = 'Hello from my new site',
}
